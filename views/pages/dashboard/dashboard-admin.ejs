<style>
    .dashboard-container {
        max-width: 1200px;
        margin: 2rem auto;
        padding: 0 1rem;
    }

    .dashboard-header {
        text-align: center;
        margin-bottom: 2rem;
    }

    .dashboard-header h1 {
        color: var(--text);
        font-size: 2rem;
    }

    .dashboard-section {
        background: var(--card-bg);
        margin-bottom: 2rem;
        padding: 1.5rem;
        border-radius: 8px;
        box-shadow: 0 2px 4px var(--shadow);
    }

    .dashboard-section h2,
    .dashboard-section a {
        font-weight: bold;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--accent);
        font-size: 1.5rem;
        margin-bottom: 1rem;
        transition: color 0.3s;
    }

    .select a:hover {
        color: #148f77;
    }

    .section-content {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    button {
        padding: 1rem;
        border: none;
        background: var(--accent);
        color: white;
        border-radius: 5px;
        cursor: pointer;
        transition: background 0.3s;
    }

    button:hover {
        background: #16a085;
    }

    @media screen and (max-width: 768px) {
        header .nav-links {
            flex-direction: column;
            align-items: flex-start;
        }

        .dashboard-container {
            padding: 0;
        }
    }
</style>

<div class="dashboard-header">
    <h1>Bienvenue, Administrateur</h1>
</div>

<section id="users" class="dashboard-section select">
    <a href="/dashboard/users"><ion-icon name="people-outline"></ion-icon> Gestion des utilisateurs</a>
</section>

<section id="products" class="dashboard-section select">
    <a href="/dashboard/products"><ion-icon name="pricetag-outline"></ion-icon> Gestion des produits</a>
</section>

<section id="carts" class="dashboard-section select">
    <a href="/dashboard/carts"><ion-icon name="cart-outline"></ion-icon> Gestion des paniers</a>
</section>

<section id="orders" class="dashboard-section select">
    <a href="/dashboard/orders"><ion-icon name="fast-food-outline"></ion-icon> Gestion des commandes</a>
</section>

<section id="reviews" class="dashboard-section select">
    <a href="/dashboard/reviews"><ion-icon name="mail-outline"></ion-icon> Gestion des commentaires</a>
</section>

<!-- <section id="supports" class="dashboard-section select">
    <a><ion-icon name="cart-outline"></ion-icon> Gestion de l'assistance</a>
</section>

<section id="stats" class="dashboard-section">
    <a><ion-icon name="stats-chart-outline"></ion-icon> Statistiques</a>
    <div class="section-content">
        <p>Afficher les statistiques de ventes et utilisateurs.</p>
    </div>
</section>

<section id="settings" class="dashboard-section">
    <a><ion-icon name="settings-outline"></ion-icon> Paramètres</a>
    <div class="section-content">
        <button>Modifier les paramètres</button>
        <button>Se déconnecter</button>
    </div>
</section> -->